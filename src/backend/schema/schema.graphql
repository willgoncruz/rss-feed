type Feed {
    url: String!
}

type Author {
    uri: String!
    name: String!
}

type Article {
    title: String!
    author: Author!
}

type Query {
    feeds: [Feed]
    articles(url: String): [Article]
}